# dependencies — never push these (thousands of files)
node_modules/

# build output — hosts rebuild it, keep the repo clean
dist/
*.tsbuildinfo

# local env files (secrets live in the Supabase dashboard instead)
.env
.env.local
.env.*.local

# OS / editor / host junk
.DS_Store
Thumbs.db
.vercel
.netlify
*.log
.next/
build/
